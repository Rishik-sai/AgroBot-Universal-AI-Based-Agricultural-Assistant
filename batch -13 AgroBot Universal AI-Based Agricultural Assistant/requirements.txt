# --- Web framework ---
Flask==3.0.3
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
Flask-SocketIO==5.3.6
SQLAlchemy==2.0.36
Werkzeug==3.0.4
Jinja2==3.1.4

# --- Production server (SocketIO needs an async worker) ---
gunicorn==22.0.0
gevent==24.2.1
gevent-websocket==0.10.1

# --- AI / integrations ---
google-genai==2.22.0
requests==2.32.3

# --- Utilities ---
python-dotenv==1.0.1
Pillow==10.4.0

# --- PostgreSQL driver (used when DATABASE_URL is set) ---
psycopg2-binary==2.9.9

# --- Pinned transitive deps ---
# google-genai + gevent are sensitive to these. Left unpinned, Render resolved
# a combination that failed at runtime with "super(type, obj): obj must be an
# instance or subtype of type" on every Gemini call, while the same
# requirements worked locally. These are the versions verified against the
# production stack (gunicorn + GeventWebSocketWorker + google-genai 2.22.0).
pydantic==2.13.5
httpx==0.28.1
anyio==4.15.0
greenlet==3.5.5
